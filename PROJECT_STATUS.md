# PROJECT_STATUS

## 当前项目状态

- 仓库名称：`survival-lab`。
- 当前项目类型：早期占位型训练仓库，目前只有基础说明文件，还没有 C++ 源码、构建脚本或测试框架。
- 当前代码规模很小，适合作为长期 C++ 学习/工程训练项目的起点。
- 本次审计只做项目体检和路线规划，不新增业务功能，不删除已有文件，不重写项目。

## 已有功能

- 已有 `README.md`，说明项目名称和用途：`survival-lab` / `training project`。
- 暂未发现 C++ 入口文件，例如 `main.cpp`。
- 暂未发现构建系统，例如 `CMakeLists.txt`、`Makefile`、Visual Studio 工程文件或构建脚本。
- 暂未发现测试目录或测试框架。

## 当前问题

- 项目还没有明确的 C++ 源码目录结构。
- 项目还没有可编译、可运行的最小程序。
- 项目还没有构建方式，无法执行标准化编译命令。
- 项目还没有测试方式，无法验证每日增量是否破坏已有行为。
- 项目还没有模块边界，后续需要逐步形成 `src/`、`include/`、`tests/` 等结构。

## 可扩展方向

建议采用小步演进方式，把仓库逐步扩展为长期 C++ 生存训练项目：

- `src/`：放置应用入口和实现文件。
- `include/`：放置可复用头文件。
- `tests/`：放置单元测试或最小回归测试。
- `docs/`：后续可放置学习笔记和设计记录。
- `CMakeLists.txt`：推荐作为跨平台构建入口。
- 小型模块方向：命令行输入输出、字符串处理、文件读写、错误处理、容器算法、简单日志、配置读取、测试驱动重构。

## 下一步最小任务

Day 01 建议只做一个最小任务：建立可编译、可运行、可测试的 C++ Hello World 骨架。

建议范围：

- 新增 `CMakeLists.txt`。
- 新增 `src/main.cpp`。
- 新增一个最小测试方式，例如 CTest smoke test，验证程序可以运行并输出预期文本。
- 不引入复杂依赖。
- 不做业务功能扩展。
